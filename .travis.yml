language: node_js

node_js:
  - 12.10

cache:
  # cache both npm modules and Cypress binary
  directories:
    - ~/.npm
    - ~/.cache

install:
  - npm run install:client
  - npm ci

jobs:
  include:
    - stage: test
      script: npm run test:cypress:component:ci
      if: type = cron
    - stage: test
      script:
        - echo 'running unit clientt'
        - npm run test:unit:client
    - stage: deploy
      install: skip
      script: skip
      deploy:
        - provider: script
          script: echo 'yep deployed cron'
          on:
            condition: type IS cron
        - provider: script
          script: echo 'branch master only'
          on:
            condition: branch IS master
        - provider: script
          script: echo 'branch asdf only'
          on:
            condition: branch IS asdf
        - provider: script
          script: 
            - echo 'yep deployed no cron'
            - npm run test:unit:client



